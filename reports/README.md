Evaluation reports are written here by the tooling in `evaluation/`.
The directory remains empty in the repository so CI and local runs can
persist their own artifacts without polluting the git history.
